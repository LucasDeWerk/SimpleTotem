# Scaffold — router/

**Vue Router** — rotas hash (`createWebHashHistory`).

## Arquivos

| Arquivo | Papel |
|---------|-------|
| `index.js` | Definição de rotas, guards admin, redirect `/` → `/totem` |

## Rotas cliente

`/totem`, `/totem/catalogo`, `/totem/carrinho`, `/totem/pagamento`, `/totem/processando`, `/totem/concluido`, `/totem/timeout`

## Rotas admin

`/admin`, `/admin/painel`, `/admin/hardware`

## Meta fields

`title`, `showBack`, `blockInteraction`, `requiresAdmin`
