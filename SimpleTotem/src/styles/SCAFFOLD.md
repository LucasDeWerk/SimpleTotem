# Scaffold — styles/

**CSS global** e design tokens do totem.

## Arquivos

| Arquivo | Papel |
|---------|-------|
| `variables.css` | Tokens — cores, fontes, espaçamentos, breakpoints |
| `global.css` | Reset, tipografia base, utilitários globais |
| `transitions.css` | Animações e transições Vue |

## Import

Carregados em `main.js` antes do mount.
